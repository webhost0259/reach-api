{
  "watch": ["workers", "src/services", "src/config"],
  "ext": "ts,json",
  "ignore": ["**/*.test.ts"],
  "exec": "ts-node -r tsconfig-paths/register ./workers/index.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
